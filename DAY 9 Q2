#include <iostream>
using namespace std;

int main() 
{
    int t;
    cin>>t;
    while(t--)
    {
        int a1,a2,a3,a4,a5,p;
        cin>>a1>>a2>>a3>>a4>>a5>>p;
        int total=(a1+a2+a3+a4+a5)*p;
        if(total/24==5 && total%24==0)
        cout<<"No"<<endl;
        else if(total/24<5)
        cout<<"No"<<endl;
        else
        cout<<"Yes"<<endl;
    }
	return 0;
}
