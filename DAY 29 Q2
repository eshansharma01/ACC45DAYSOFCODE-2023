#include <iostream>
using namespace std;

int main() 
{
    int n;
    cin>>n;
    int i,sum,j;
    while(n--)
    {
        sum=0;
        j=5;
        while(j--)
        {
            cin>>i;
            sum=sum+i;
        }
        if(sum==0)
        cout<<"Beginner"<<endl;
        else if(sum==1)
        cout<<"Junior Developer"<<endl;
        else if(sum==2)
        cout<<"Middle Developer"<<endl;
        else if(sum==3)
        cout<<"Senior Developer"<<endl;
        else if(sum==4)
        cout<<"Hacker"<<endl;
        else
        cout<<"Jeff Dean"<<endl;
    }
	return 0;
}
